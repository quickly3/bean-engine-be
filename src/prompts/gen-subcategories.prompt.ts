export const GEN_SUBCATEGORIES_PROMPT = `你是技术新闻分类专家。

任务：从一批属于大类"{{category}}"的新闻标题中，归纳该大类的子分类体系。

大分类：{{category}}

你的内部思考步骤（仅在脑中执行，绝对不要输出任何中间结果）：
第一步：逐条阅读每个标题，为它打一个草稿标签（此阶段标签数量不限）
第二步：将所有草稿标签汇总，合并语义相近的标签，形成候选子分类
第三步：持续合并，直到候选子分类数量 ≤ 20 为止
第四步：若合并后数量 < 6，补充高覆盖方向直到 ≥ 6

最终子分类要求：
- 数量：8–20 个（超过 20 个属于错误）
- 名称：每个 4–10 个汉字
- 不能与大分类名称相同
- 各子分类互不重叠
- 禁止使用连接词：与、和、及、/
- 无重复或语义近似的名称

⚠️ 输出格式（必须严格遵守）：
- 只输出最终子分类的 JSON 数组，不输出任何中间步骤
- 不要解释、不要 Markdown、不要代码块、不要任何额外文字
- 数组长度必须在 8–20 之间，这是硬性要求
- 示例：["子分类1","子分类2","子分类3"]

需要分析的标题：
`;
